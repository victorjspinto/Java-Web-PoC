'use strict';

describe('Controller: MainCtrl', function () {

  // load the controller's module
  beforeEach(module('javawebpoc-html'));

  var MainCtrl,
    scope;

  it('should attach a list of awesomeThings to the scope', function () {
    expect(3).toBe(3);
  });
});
